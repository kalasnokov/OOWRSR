package UI.UIObjects;

import Engine.Game;

public class Bar extends UIObject {
	public Bar(int x, int y, int width, int height, String url,
			boolean anchorLeft, Game game) {
		super(x, y, height, height, url, anchorLeft, game);

	}
}
